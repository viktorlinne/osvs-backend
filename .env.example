# Copy your existing .env values here for local development when running with Docker.
# Important: Do NOT commit secrets to version control in real projects.

# -- Database (MySQL)
# When running via Docker Compose the `DB_HOST` below points to the service name `db`.
DB_HOST=localhost
DB_USER=root
DB_NAME=osvs
DB_PASS=

# Optional single URL connection string used by some tools/migrations (leave empty or override)
# Example: mysql://root:password@127.0.0.1:3306/osvs
DATABASE_URL=

# -- Argon2 password hashing configuration
ARGON2_MEMORY_KB=65536
ARGON2_TIME=3
ARGON2_PARALLELISM=1

# -- Server
PORT=4000
NODE_ENV=development

# -- JWT / Auth
JWT_SECRET=
ACCESS_COOKIE=osvs_access
REFRESH_COOKIE=osvs_refresh
COOKIE_DOMAIN=localhost

# -- Brevo (transactional email)
BREVO_API_KEY=
BREVO_FROM=
MAIL_SEND_CONCURRENCY=5

# -- Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# -- Cache / Redis
# When running via Docker Compose the REDIS_URL below points to the service name `redis`.
REDIS_URL=redis://redis:6379
CACHE_TTL_SECONDS=60

# -- Request timing thresholds (ms)
SLOW_REQUEST_THRESHOLD_MS=500
SLOW_QUERY_THRESHOLD_MS=200

# Notes:
# - Fill secrets (JWT_SECRET, STRIPE_*, BREVO_API_KEY, DB_PASS, DATABASE_URL) in your local
#   `.env` file. Do NOT commit real secrets into version control.
